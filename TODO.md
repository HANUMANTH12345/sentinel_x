# TODO - Run the project

- [ ] Inspect Docker/compose setup and determine correct ports/URLs
- [ ] Fix Postgres init issue (database name mismatch) so containers start cleanly
- [ ] Start the project with `docker compose up --build`
- [ ] Verify frontend is reachable (http://localhost:4173)
- [ ] Verify API health endpoint works (http://localhost:5000/api/health)
- [ ] Confirm key route (e.g., /api/threatmap) returns data

